---
const roles = [
  {
    title: 'Software Engineer',
    dates: 'Aug 2021 – Jan 2023',
    scope: 'Backend engineering',
    proof: 'Rebuilt the device sync from 12–15 hours to under one hour and built the audit-trail engine.',
  },
  {
    title: 'Software Engineer (L2)',
    dates: 'Jan 2023 – Jul 2024',
    scope: 'Feature ownership',
    proof: 'Moved from implementing specifications to writing and owning them across four product initiatives.',
  },
  {
    title: 'Senior Software Engineer',
    dates: 'Jul 2024 – Jul 2026',
    scope: 'Technical lead',
    proof: 'Led delivery across Workflow Automation, CMDB / IT Graph, and AWS cloud integration.',
  },
  {
    title: 'Principal Software Engineer',
    dates: 'Jul 2026 – now',
    scope: 'Architecture and leverage',
    proof: "Reviews, production escalation, technical direction, and tooling that changes how the wider team ships.",
    current: true,
  },
];

// Career progression and internal leverage are one story: the role grew from delivering
// features to improving the system around delivery. Keeping both in this section prevents
// two consecutive, near-identical timeline sections on the homepage.
const leverage = [
  {
    title: 'Conventions that arrive with the branch',
    meta: '12 rules · 3 skills · merged June 2026',
    body: 'Repository-level Cursor rules and coding-agent skills encode the Rails and React conventions engineers used to reconstruct from memory. Every branch carries them; there is nothing separate to install or keep in sync.',
  },
  {
    title: 'Guardrails built into setup',
    meta: '3 hooks · 4 products · automatic updates',
    body: 'Pre-commit and pre-push checks catch newly added debugger statements, conflict markers, lockfile drift, and Ruby syntax errors. A third hook attaches the ticket reference to the commit message automatically.',
  },
  {
    title: 'Performance feedback at the source',
    meta: 'Bullet · Slack · staging',
    body: 'N+1 warnings leave the log and reach the team with the product, request URL, suggested fix, stack frames, and a link to the offending GitHub line before the regression reaches production.',
  },
];
---

<section id="trajectory" class="bg-surface border-t border-line">
  <div class="band shell trajectory-grid">
    <div class="min-w-0">
      <div class="measure">
        <p class="marker mb-6">Career and leverage</p>
        <h2 class="t-display-2 text-paper">
          The role grew. The leverage stayed.
        </h2>
      </div>

      <ol class="mt-10 flex flex-col border-b border-line">
        {roles.map((role) => (
          <li class:list={['career-step border-t border-line py-5', role.current && 'is-current']}>
            <div class="flex flex-wrap items-baseline justify-between gap-x-5 gap-y-1">
              <h3 class="font-display wonk font-semibold text-xl text-paper">{role.title}</h3>
              <p class="font-mono text-[11px] text-quiet shrink-0">{role.dates}</p>
            </div>
            <p class="mt-2 t-micro">{role.scope}</p>
            <p class="mt-2 text-sm leading-relaxed text-paper/70">{role.proof}</p>
          </li>
        ))}
      </ol>
    </div>

    <div class="min-w-0 xl:pt-1">
      <p class="t-micro mb-3">What stayed behind</p>
      <p class="max-w-[34rem] text-paper/70 leading-relaxed">
        The useful part of a promotion is the leverage it leaves in the system. These three
        changes now travel with the repository or surface where the team already works.
      </p>

      <ol class="mt-8 grid gap-4">
        {leverage.map((item, index) => (
          <li data-reveal class="impact-card rounded-lg border border-line bg-ink/45 p-6 sm:p-7">
            <div class="flex items-start gap-4">
              <span aria-hidden="true" class="font-mono text-xs text-signal pt-1">
                {String(index + 1).padStart(2, '0')}
              </span>
              <div>
                <h3 class="t-display-3 text-paper">{item.title}</h3>
                <p class="mt-2 font-mono text-[11px] text-quiet">{item.meta}</p>
                <p class="mt-4 text-sm leading-relaxed text-paper/75">{item.body}</p>
              </div>
            </div>
          </li>
        ))}
      </ol>
    </div>
  </div>
</section>

<style>
  .trajectory-grid {
    display: grid;
    gap: 4rem;
  }

  @media (min-width: 1280px) {
    .trajectory-grid {
      grid-template-columns: minmax(0, 0.88fr) minmax(0, 1.12fr);
      gap: 6rem;
      align-items: start;
    }
  }

  .career-step {
    position: relative;
    padding-left: 1.75rem;
  }

  .career-step::before {
    content: '';
    position: absolute;
    left: 0;
    top: 1.75rem;
    width: 0.625rem;
    height: 0.625rem;
    border: 1px solid var(--color-line-strong);
    border-radius: 9999px;
    background: var(--color-surface);
  }

  .career-step.is-current::before {
    border-color: var(--color-signal);
    background: var(--color-signal);
    box-shadow: 0 0 0 5px var(--color-signal-dim);
  }

  .impact-card {
    transition: border-color 0.2s ease, background-color 0.2s ease;
  }

  .impact-card:hover {
    border-color: var(--color-line-strong);
    background: color-mix(in srgb, var(--color-ink) 65%, transparent);
  }
</style>
